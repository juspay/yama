name: CI

on:
  push:
    branches: [main]
    # Yama's learn commits touch only its own knowledge files and carry
    # [skip ci]. This is the second guard: [skip ci] is honoured inconsistently
    # across CI systems, and a knowledge update has nothing to build or test.
    paths-ignore:
      - ".yama/knowledge/**"
      - ".yama/product/**"
      - ".yama/state/**"
      - ".yama/reports/**"
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22, 26]

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup PNPM
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
        with:
          version: 10.14.0

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ matrix.node-version }}
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Check formatting
        # Check, never write: `pnpm run format` reformatted the checkout in place
        # and then passed, so CI could never tell a formatted branch from an
        # unformatted one.
        run: pnpm run format:check

      - name: Run linting
        run: |
          echo "🔍 Running ESLint validation with controlled warning limits..."
          pnpm run lint

      - name: 🔒 Security & Environment Validation
        run: |
          echo "🔍 Running build rule enforcement validations..."
          pnpm run validate:all

      - name: Type checking
        run: pnpm run type-check

      - name: Build package
        run: pnpm run build

      - name: Run tests
        run: pnpm run test

  build-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup PNPM
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
        with:
          version: 10.14.0

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "26"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Check package build
        run: pnpm run prepack

      - name: Verify package contents
        run: |
          pnpm pack
          ls -la dist/

  quality-gate:
    runs-on: ubuntu-latest
    name: 🛡️ Code Quality & Security Gate
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Setup PNPM
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
        with:
          version: 10.14.0

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "26"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: 🔍 Build Validation
        run: |
          echo "🔍 Running comprehensive build validation..."
          pnpm run validate

      - name: 📝 Commit Message Validation
        # Pass attacker-controllable PR context through env vars, never interpolated
        # into the shell (a branch name or ref can carry shell metacharacters).
        env:
          EVENT_NAME: ${{ github.event_name }}
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          echo "📝 Validating commit message format..."
          # For PRs, validate the actual PR head commit, not the merge commit
          if [ "$EVENT_NAME" = "pull_request" ]; then
            echo "PR Event: Getting head commit from $HEAD_REF"
            echo "Head SHA: $HEAD_SHA"
            # Use GitHub context to get the commit message directly
            COMMIT_MSG=$(git log -1 --pretty=format:"%s" "$HEAD_SHA")
            echo "Validating PR head commit: $COMMIT_MSG"
          else
            COMMIT_MSG=$(git log -1 --pretty=format:"%s")
            echo "Validating push commit: $COMMIT_MSG"
          fi
          node scripts/commit-validation.cjs "$COMMIT_MSG"

      - name: 🌍 Environment Validation
        run: |
          echo "🌍 Validating environment configuration..."
          pnpm run validate:env

      - name: 🔒 Security Validation
        run: |
          echo "🔒 Running security checks..."
          pnpm run validate:security

      - name: 🛡️ Dependency Audit (production)
        run: |
          echo "🛡️ Auditing production dependencies (advisory — see docs/dependency-audit.md)..."
          pnpm audit --prod || true
        continue-on-error: true

      - name: 📊 ESLint Quality Report
        run: |
          echo "📊 Generating ESLint quality report..."
          pnpm run lint --format=stylish
        continue-on-error: true

      - name: 📈 TypeScript Compiler Check
        run: |
          echo "📈 Running TypeScript compiler strict checks on source files..."
          pnpm run type-check

      - name: 🎯 Code Coverage Analysis
        run: |
          echo "🎯 Running tests with coverage..."
          pnpm run test --coverage || echo "Tests completed with warnings"
        continue-on-error: true

      - name: 📋 Quality Summary
        run: |
          echo "📋 Build Rule Enforcement Summary:"
          echo "✅ Build validation passed"
          echo "✅ Environment validation passed" 
          echo "✅ Security validation passed"
          echo "✅ TypeScript compilation passed"
          echo "✅ Commit message format validated"
          echo ""
          echo "🎉 All quality gates passed!"
